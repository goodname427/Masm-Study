DATAS SEGMENT
    ARR DB 100 DUP(?)
    N EQU $ - DATAS
DATAS ENDS

STACKS SEGMENT
    
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    MOV BX,OFFSET ARR
    MOV DL,0
    MOV CX,N
LOOP_MAIN:
    MOV [BX],DL
    INC BX
    INC DL
    
    LOOP LOOP_MAIN
    
    MOV AH,4CH
    INT 21H
CODES ENDS

END START