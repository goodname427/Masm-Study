DATAS SEGMENT
    X DW 1234H
    Y DW 2345H
    Z DW -1234H
    W DW ?
DATAS ENDS

STACKS SEGMENT
    STACK DW 100H DUP(?)
    TOP DW LENGTH STACK
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    MOV AX,STACKS
    MOV SS,AX
    MOV AX,TOP
    MOV SP,AX

    MOV AX,X
    PUSH AX
    MOV AX,Y
    PUSH AX
    MOV AX,Z
    PUSH AX

    CALL MAX
    MOV W,AX

    MOV AH,4CH
    INT 21H


MAX PROC
    MOV BP,SP
    ADD BP,2
    MOV AX,[BP]

    MOV CX,2
LOOP_COMPARE:
    ADD BP,2
    CMP AX,[BP]
    JG LOOP_COTINUE
    MOV AX,[BP]
LOOP_COTINUE:
    LOOP LOOP_COMPARE
    
    RET
MAX ENDP

    
    
    
CODES ENDS

END START